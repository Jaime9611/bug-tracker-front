import { render } from '@testing-library/react';

import App from './app';

describe('App', () => {
  it('render hello world', () => {
    // ARRANGE
    render(<App />);
    // ACT
    // ASSERT
  });
});
